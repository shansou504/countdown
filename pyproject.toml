[project]
name = "countdown"
version = "1.0.0"
description = "Web Based Countdown Display."
dependencies = [
    "flask"
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"